/*
 * regex_shape.h -- Cypher regex-shaped string predicate bridge.
 */
#ifndef CBM_CYPHER_REGEX_SHAPE_H
#define CBM_CYPHER_REGEX_SHAPE_H

#include <stdbool.h>

bool cbm_cypher_looks_like_regex(const char *s);

#endif
